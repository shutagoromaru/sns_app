<!DOCTYPE html>
<html>
<head>
  <title>New Topic</title>
</head>
<body>
  <h1>New Topic</h1>
  
  <%= form_with(url: creat_topic_path, model: @topic, local: true, method: :post) do |f| %>
  <div>
    <%= f.label :title, "Title" %>
    <%= f.text_field :title %>
  </div>

  <div>
    <%= f.submit "Create" %>
  </div>
<% end %>

  <!-- This form is just a placeholder and will not actually work. Replace with your form_for or form_with -->
  <form action="/topics" method="post">
    <label for="title">Title</label>
    <input type="text" id="title" name="title">

    <label for="body">Body</label>
    <textarea id="body" name="body"></textarea>

    <input type="submit" value="Create Topic">
  </form>
</body>
</html>